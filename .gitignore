# Environment
.env
venv/
env/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/

# OS
Thumbs.db
Desktop.ini
.DS_Store

# ── INTERVUE ──────────────────────────────────────────────
# Raw data is NOT pushed to GitHub (too large, ~4.6GB).
# Keep it local; upload to Kaggle as a Dataset for training.
data/raw/
data/processed/
data/splits/
data/tokenized/
data/sessions/

# Trained artifacts / checkpoints (regenerate via training)
models/*/saved/
models/generator/saved/
*.pt
*.pkl
*.npy

# Tokenizer output (regenerate)
tokenizer/saved/

# Junk duplicate scripts (Windows browser downloads)
download_more_data*(*.py
data_pipeline/download_more_data(1).py
data_pipeline/download_more_data(2).py
data_pipeline/download_more_data_1_.py
data_pipeline/download_more_data_2_.py
# Run artifacts
results/
smoke_run/
